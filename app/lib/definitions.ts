export type DiscountInfo = {
    img_url: string,
    item_name: string,
    discount: number,
    remaining_days: number
}

export type ItemInfo = {
    name: string,
    address: string,
    price: number,
    img_url: string
}